{{#each posts as |post|}}
  <div class='row mb-4 project'>
    <div class='col-md-5'>
      <h2>
        <a href='/project/{{post.id}}'>{{post.post_title}}</a>
      </h2>
    </div>
    <div class='col-md-7'>
      <p>
        {{post.post_content}}
      </p>
    </div>
  </div>
{{/each}}